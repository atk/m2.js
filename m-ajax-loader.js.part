,
	// convenient AJAX loading
	load: function (u, t, c) {
		t = this,
		c = function (h) {
			h = this.responseText
			t.each(function (n) { n.innerHTML = h })
		}
		m.ajax(u.url ? (u.callback = c, u) : { url: u, callback: c });
	}
